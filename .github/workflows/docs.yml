name: docs

on:
  push:
    branches:
      - master
      - dev
  pull_request:

jobs:
  docs-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Install
        run: pip install -e . --group docs

      - name: Build
        run: sphinx-build -b html source build -D nb_execution_mode=off --keep-going
        working-directory: docs
